@model IEnumerable<Type_D.Models.Service>

@{
    ViewBag.Title = "Услуги";
}

<h2>Услуги</h2>
<ul>
    @foreach (var service in Model)
    {
        <li>
            <a href="@Url.Action("Details", new { id = service.Id })">@service.Name</a> - @service.Price руб.
        </li>
    }
</ul>
